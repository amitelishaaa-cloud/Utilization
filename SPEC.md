# SPEC.md — Utilization: כלי ניהול ניצול לפרילאנסרים

## 1. תקציר

כלי SaaS לפרילאנסרים בודדים (ללא עובדים) שמספק **תחזית ניצול ועסקאות קדימה** — לא רק ניתוח של מה שקרה, אלא מה צפוי לקרות — כדי לאפשר פעולה בזמן לפני שבור בהכנסות הופך לבלתי-ניתן לתיקון.

הכלי ממזג שלוש שכבות: קיבולת זמינה, עבודה מחויבת, ועסקאות בתהליך — ומייצר מסך cockpit אחד עם המלצת פעולה קונקרטית.

---

## 2. קהל יעד ובעיה

**קהל יעד:** פרילאנסר בודד (עצמאי) בכל תחום שעובד לפי שעות או פרויקטים. לא סוכנויות, לא צוותים.

**הבעיה:** פרילאנסרים גולשים בין תת-ניצול לעומס יתר כי הם לא רואים את הצפוי מספיק זמן קדימה. עד שמגלים חור בהכנסות — כבר מאוחר מדי לאסוף לקוחות חדשים.

**החידוש:** מנגנון שמשקלל עסקאות בתהליך לפי הסתברות שלב — ולא רק את מה שכבר נחתם — כדי לתת תמונה מציאותית של העתיד.

**כלים מתחרים:** Float, Forecast, Runn — כולם בנויים לצוותים וסוכנויות, לא לפרילאנסר יחיד. זו פינה ריקה בשוק.

---

## 3. ארכיטקטורת נתונים (Schema)

### 3.1 `users`

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `email` | text unique | |
| `google_id` | text nullable | OAuth |
| `default_weekly_hours` | float | קיבולת ברירת מחדל לשבוע |
| `plan` | enum: `free` / `pro` | |
| `plan_expires_at` | timestamptz nullable | |
| `created_at` | timestamptz | |

### 3.2 `clients`

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid FK → users | |
| `name` | text | |
| `created_at` | timestamptz | |

יחס: לקוח אחד → כמה פרויקטים/התקשרויות מקבילות.

### 3.3 `projects` (עבודה מחויבת — פרויקטים חד-פעמיים)

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid FK | |
| `client_id` | uuid FK → clients | |
| `name` | text | |
| `pricing_type` | enum: `hourly` / `fixed` | |
| `estimated_hours` | float | סה"כ שעות מוערכות |
| `actual_hours` | float nullable | לניתוח רטרוספקטיבי |
| `hourly_rate` | decimal nullable | אם `pricing_type = hourly` |
| `fixed_price` | decimal nullable | אם `pricing_type = fixed` |
| `start_date` | date | |
| `end_date` | date | חובה — גם לפרויקטים פתוחים (ניתן לסמן כהערכה) |
| `is_end_date_estimated` | boolean | default `false` — מסמן אם התאריך הוא הערכה גסה שעשויה להשתנות |
| `status` | enum: `active` / `completed` / `cancelled` | |
| `created_at` | timestamptz | |

**סיום פרויקט:** המערכת מזהה אוטומטית שה-`end_date` עבר ומציגה "נראה שהפרויקט הסתיים — לסמן כהושלם?" למשתמש לאישור.

### 3.4 `project_weekly_allocations` (חלוקה ידנית לשבועות — אופציונלי)

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `project_id` | uuid FK → projects | |
| `week_start` | date | יום שני של השבוע |
| `allocated_hours` | float | עוקף את ברירת המחדל |

אם אין רשומה — המערכת מחלקת את `estimated_hours` שווה בשווה על כל שבועות הפרויקט.

### 3.5 `retainers` (התקשרויות שוטפות — לקוחות רטיינר)

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid FK | |
| `client_id` | uuid FK → clients | |
| `name` | text | |
| `monthly_hours` | float | שעות חוזרות בחודש |
| `pricing_type` | enum: `hourly` / `fixed_monthly` | |
| `hourly_rate` | decimal nullable | |
| `monthly_fixed_price` | decimal nullable | |
| `start_date` | date | |
| `end_date` | date nullable | null = פתוח |
| `status` | enum: `active` / `ended` | |
| `created_at` | timestamptz | |

המערכת ממירה `monthly_hours` לשעות שבועיות (`monthly_hours / 4.33`) לחישוב ניצול.

### 3.6 `capacity_exceptions` (שבועות חריגים)

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid FK | |
| `week_start` | date | יום שני של השבוע |
| `available_hours` | float | עוקף את ברירת המחדל הגלובלית |
| `reason` | text nullable | "חופשה", "חג", חופשי |
| `created_at` | timestamptz | |

### 3.7 `pipeline_deals` (עסקאות בתהליך)

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid FK | |
| `client_id` | uuid FK → clients nullable | |
| `name` | text | |
| `pricing_type` | enum: `hourly` / `fixed` | |
| `estimated_hours` | float | |
| `hourly_rate` | decimal nullable | |
| `fixed_price` | decimal nullable | |
| `expected_start_date` | date | |
| `expected_end_date` | date | |
| `current_stage` | enum (ראה §4.1) | |
| `probability_override` | float nullable | עוקף הסתברות ברירת מחדל |
| `status` | enum: `active` / `won` / `lost` | |
| `created_at` | timestamptz | |
| `closed_at` | timestamptz nullable | בעת מעבר ל-won/lost |

**מגבלת Free tier:** משתמש חינמי יכול לקיים עד 3 עסקאות `active` במקביל.

### 3.8 `pipeline_stage_history` (היסטוריית שלבים)

| עמודה | סוג | הערות |
|---|---|---|
| `id` | uuid PK | |
| `deal_id` | uuid FK → pipeline_deals | |
| `from_stage` | text nullable | null בכניסה ראשונה |
| `to_stage` | text | |
| `changed_at` | timestamptz | |

נשמר **כל** מעבר שלב עם חותמת זמן — הבסיס למנגנון Bayes האישי בשלב הבא.

---

## 4. לוגיקת ליבה

### 4.1 הסתברויות שלב (ברירות מחדל)

| שלב | `current_stage` | הסתברות |
|---|---|---|
| פנייה ראשונית | `inquiry` | 10% |
| הצעה נשלחה | `proposal` | 30% |
| משא ומתן | `negotiation` | 55% |
| סגר בעל פה | `verbal_close` | 80% |
| חוזה / יומן | `contract` | 100% |

ניתן לעקוף ידנית דרך `probability_override` לכל עסקה.

### 4.2 חישוב קיבולת לשבוע

```
capacity(week) =
  capacity_exceptions.available_hours   // אם קיים רשומה
  OR users.default_weekly_hours          // ברירת מחדל גלובלית
```

### 4.3 חישוב שעות מחויבות לשבוע

```
committed(week) =
  Σ project_weekly_allocations.allocated_hours   // אם הוגדר ידנית
  OR project.estimated_hours / total_project_weeks  // חלוקה שווה

  + Σ retainer.monthly_hours / 4.33              // לכל רטיינר פעיל
```

`total_project_weeks = (project.end_date - project.start_date) / 7`. מאחר ש-`end_date` חובה בכל פרויקט, ערך זה תמיד מוגדר — אין null case לטיפול.

### 4.4 חישוב שעות pipeline משוקללות לשבוע

```
pipeline(week) =
  Σ (deal.estimated_hours / deal_weeks * stage_probability)
  // לכל עסקה שה-expected_start_date–expected_end_date חופפים ל-week
```

`stage_probability` = `probability_override` אם קיים, אחרת ברירת מחדל לפי שלב.

### 4.5 ניצול צפוי

```
utilization(period) =
  (committed_hours_in_period + pipeline_hours_in_period) / capacity_in_period
```

ה-hero metric בקוקפיט הוא `utilization(next_month)` — מוצג כאחוז.

### 4.6 לוגיקת המלצות (rule-based, ללא קלט משתמש)

מחושב לכל חודש בטווח 3 חודשים קדימה. על בסיס ניצול צפוי לכל חודש:

| מצב | ניצול צפוי | תג צבעוני | המלצה |
|---|---|---|---|
| עומס יתר | > 110% | אדום | "שקול לדחות לידים חדשים או להעלות מחיר לעסקאות שטרם נסגרו" |
| ניצול מיטבי | 80%–110% | ירוק | "ניצול טוב — אין צורך בפעולה" |
| אזהרה | 50%–80% | צהוב | "שיווק נמרץ — פנה ללידים חדשים" |
| פער דחוף | < 50%, חודש 1 | אדום כהה | "חור בהכנסה החודש הבא — מאוחר לסגור לידים. שקול פרויקטים קצרים / הלוואת גשר" |
| פער בינוני | < 50%, חודש 2 | כתום | "שיווק דחוף — חלון הזמן לסגירת עסקאות מצטמצם" |
| פער רחוק | < 50%, חודש 3+ | צהוב | "שיווק נמרץ — יש זמן לפעול" |
| ניצול גבוה קבוע | > 90% לחודשיים+ | כחול | "ניצול גבוה לאורך זמן — שקול להעלות מחירים" |

ההמלצה המוצגת היא של **החודש הקרוב ביותר עם בעיה**. אם הכל תקין — מוצגת ההמלצה הירוקה.

### 4.7 טיפול ב-Overbooking בפועל

אם הניצול הצפוי עולה על 100% (למשל כל עסקאות ה-pipeline נסגרות), המערכת **מציגה את המצב עם ההמלצה בלבד** — לא מבצעת פעולה אוטומטית. המשתמש מחליט.

### 4.8 פירוס שעות פרויקט על שבועות

**ברירת מחדל:** חלוקה שווה (`estimated_hours / total_project_weeks`).

**אופציה ידנית:** המשתמש יכול להגדיר שעות שבועיות ספציפיות דרך `project_weekly_allocations`. שבועות שאין להם רשומה יקבלו את החלוקה השווה של השאר.

---

## 5. MVP — מה בפנים ומה בחוץ

### ✅ בפנים ב-MVP

- קלט ידני מלא (ללא אינטגרציות)
- הסתברויות שלב קבועות (לא אישיות)
- המלצות rule-based (לא ML)
- ניצול צפוי ל-3 חודשים קדימה
- שני סוגי עבודה מחויבת: פרויקטים + רטיינרים
- ישות לקוח עצמאית (1:N עם פרויקטים)
- היסטוריית מעברי שלבים ב-pipeline
- מטבע יחיד: ₪
- שפת UI: עברית (RTL)
- Auth: Google OAuth + אימייל/סיסמה
- Free tier: עד 3 עסקאות pipeline פעילות
- Pro tier: ₪39/חודש או ₪390/שנה

### ❌ נדחה לשלב הבא

- Bayesian smoothing אישי (מנגנון ליבה עתידי)
- דעיכת הסתברות לעסקאות תקועות
- אינטגרציה ליומן Google
- אינטגרציה לכלי חשבוניות
- התראות אימייל/push
- מולטי-מטבע
- i18n (אנגלית)
- קלט משך מחזור מכירה אישי
- לוגיקת עדיפויות בין עסקאות מתחרות

---

## 6. UX ומסכים

### 6.1 Auth

- מסך כניסה עם שני אפשרויות: "כניסה עם Google" + טופס אימייל/סיסמה
- הרשמה: אותו מסך (toggle "הרשמה / כניסה")
- אחרי auth ראשון → onboarding מינימלי

### 6.2 Onboarding

שלב יחיד חובה לפני הכניסה לקוקפיט:

> **"כמה שעות אתה עובד בשבוע רגיל?"**
> שדה מספרי — ברירת מחדל 40h
> כפתור "בוא נתחיל"

שאר ההגדרות (שבועות חריגים וכו') נגישות דרך הגדרות מאוחר יותר.

### 6.3 Cockpit (דף ראשי)

**Empty state** (לפני הכנסת נתונים):
- Placeholder עם הודעה: "הכלי יתחיל לעבוד ברגע שתוסיף פרויקט ראשון"
- כפתור בולט: "+ הוסף פרויקט"

**מצב פעיל:**

```
┌─────────────────────────────────────────────────┐
│  [תג המלצה צבעוני]  "שיווק נמרץ — פנה ללידים"  │
├─────────────────────────────────────────────────┤
│           ניצול צפוי — חודש הבא                 │
│                    73%                           │
│              [selector: 1M | 3M | מותאם]        │
├─────────────────────────────────────────────────┤
│  [עמודות חודשיות/שבועיות צבעוניות]              │
│  יולי     אוגוסט    ספטמבר    אוקטובר           │
│  ████▒░   ███░░░   ██░░░░░   ░░░░░░░            │
│  88%       61%      34%       0%                 │
├─────────────────────────────────────────────────┤
│  [Gantt — שורות פרויקטים + עסקאות pipeline]     │
│  ▓▓▓▓▓▓▓▓ פרויקט A (לקוח: X)                   │
│  ░░░░░░░░ עסקה B — משא ומתן (55%)               │
└─────────────────────────────────────────────────┘
```

**צבעי ניצול:**
- 0–49%: אדום / כתום
- 50–79%: צהוב
- 80–110%: ירוק
- >110%: אדום כהה (overbooking)

**בחירת אופק זמן:** tabs — "חודש הבא" / "3 חודשים" / "מותאם אישית" (date range picker).

**Free tier — מנגנון blur על התחזית (החלטה סופית):**

- **ה-hero metric** (אחוז הניצול הראשי — "חודש הבא") נשאר גלוי למשתמש חינמי — זו ה"טעימה" שמייצרת תיאבון לשדרוג.
- **שכבת התחזית ל-3 חודשים** (עמודות חודשיות + Gantt + תג המלצה) מוצגת עם CSS blur על גבי התוכן.
- מעל שכבת ה-blur — CTA: **"שדרג לפרו לראות את התחזית המלאה"** + כפתור "שדרג".
- הלוגיקה מבוססת על `users.plan` הקיים — אין שדה DB נוסף נדרש.

### 6.4 ניהול פרויקטים

**רשימת פרויקטים:** כרטיסים עם סטטוס, לקוח, תאריכים, % השלמה (שעות בפועל / מוערכות).

**טופס פרויקט חדש/עריכה — שדות חובה:**
- שם הפרויקט
- לקוח (select מהרשימה, עם אפשרות "הוסף לקוח חדש" inline)
- סוג תמחור: לפי שעה / מחיר קבוע
  - אם שעה: מחיר/שעה + שעות מוערכות
  - אם קבוע: סכום כולל + שעות מוערכות
- תאריך התחלה (חובה)
- תאריך סיום (חובה, ניתן לסמן כהערכה)
  - טקסט עזר: "לא בטוח? תן הערכה — תמיד אפשר לעדכן אחר כך"
  - checkbox: "תאריך זה הוא הערכה גסה" (שדה `is_end_date_estimated`)

**שדות אופציונליים:**
- שעות בפועל (לעדכון ידני שוטף)
- הערות

**אחרי שמירה:** אפשרות להגדיר חלוקת שעות שבועית מותאמת (expand section).

**סיום פרויקט:** כשה-`end_date` עובר, המערכת מציגה banner: "הפרויקט '{שם}' נראה שהסתיים — לסמן כהושלם?" עם כפתורי "כן" / "עוד לא".

### 6.5 ניהול רטיינרים

**טופס רטיינר חדש:**
- שם / תיאור
- לקוח
- שעות חודשיות
- סוג תמחור: שעה / מחיר חודשי קבוע
- תאריך התחלה
- תאריך סיום (אופציונלי)

### 6.6 Pipeline

**תצוגה:** Kanban board עם 5 עמודות לפי שלב. כרטיסים מכילים: שם, לקוח, שעות, תאריכים, הסתברות.

**הוספת עסקה חדשה:**
- שם, לקוח, שעות מוערכות, תאריכים צפויים, שלב ראשוני, תמחור
- אפשרות לעקוף הסתברות ידנית

**מעבר שלב:** גרירה בין עמודות (drag & drop) — או כפתור "קדם שלב". כל מעבר נשמר ב-`pipeline_stage_history`.

**סגירת עסקה:**
- "נסגר!" → status = `won` → עובר לפרויקטים מחויבים עם נתונים מהעסקה
- "נפל" → status = `lost` → נשמר בהיסטוריה

**Free tier gate:** כשמשתמש חינמי מנסה להוסיף עסקה 4+ — כפתור ה"הוסף עסקה" מוצג עם מנעול, ולחיצה עליו פותחת modal: "הגעת ל-3 העסקאות של התוכנית החינמית. שדרג לפרו ועקוב אחר עסקאות ללא הגבלה" + CTA "שדרג לפרו".

### 6.7 הגדרות

- שעות ברירת מחדל לשבוע
- יומן חריגים (לוח שנה לסימון שבועות חופש/חג עם שעות ידניות)
- ניהול מינוי (שדרוג/ביטול)
- מחיקת חשבון

---

## 7. מודל עסקי

### תמחור

| | חינם | פרו |
|---|---|---|
| **מחיר** | ₪0 | ₪39/חודש **או** ₪390/שנה |
| מעקב ניצול היסטורי | ✓ | ✓ |
| פרויקטים מחויבים | ✓ ללא הגבלה | ✓ ללא הגבלה |
| רטיינרים | ✓ ללא הגבלה | ✓ ללא הגבלה |
| Pipeline | עד 3 עסקאות | ✓ ללא הגבלה |
| תחזית 3 חודשים קדימה | ✗ מוצג מטושטש (blur) עם CTA לשדרוג | ✓ |
| המלצות פעולה | ✗ מוצג מטושטש (blur) עם CTA לשדרוג | ✓ |

### עקרונות

- **הגבלה לפי פיצ'ר, לא לפי זמן:** trial קצר לא מספיק להוכיח ערך. המגבלה היא מספר עסקאות (פיצ'ר ישיר), לא תקופת ניסיון.
- **עוגן תמחור:** ₪39/חודש = פחות מחצי שעת עבודה של רוב הפרילאנסרים.
- **מיצוב:** "capacity planning לאחד" — פינה שאין בה מתחרה ישיר לפרילאנסר בודד.

---

## 8. ארכיטקטורה טכנית

### Stack

- **Framework:** Next.js 16 (App Router, Turbopack)
- **Language:** TypeScript
- **Styling:** Tailwind CSS 4, RTL כברירת מחדל
- **Database & Auth:** Supabase (PostgreSQL + Auth מובנה)
- **Rendering:** Server Components כברירת מחדל, `'use client'` רק בגבולות אינטראקטיביות

### מבנה תיקיות

```
app/
  (auth)/
    login/page.tsx           // Google OAuth + email/password
    callback/route.ts        // Supabase OAuth callback
  (app)/
    layout.tsx               // auth guard, nav, RTL wrapper
    onboarding/page.tsx      // הגדרת קיבולת ראשונית
    dashboard/page.tsx       // cockpit — Server Component
    projects/
      page.tsx               // רשימת פרויקטים
      new/page.tsx
      [id]/edit/page.tsx
    retainers/
      page.tsx
      new/page.tsx
      [id]/edit/page.tsx
    pipeline/
      page.tsx               // Kanban board — Client Component
      [id]/page.tsx
    clients/page.tsx
    settings/page.tsx
  layout.tsx
  page.tsx                   // redirect → dashboard or login

lib/
  supabase/
    server.ts                // server client (service role)
    client.ts                // browser client (anon key)
  calculations/
    utilization.ts           // חישובי ניצול וניצול צפוי
    recommendations.ts       // לוגיקת המלצות
```

### Supabase

- **Auth:** מופעל דרך Supabase Auth — תומך Google OAuth + email/password מהקופסה
- **RLS:** כל טבלה מכילה `user_id`. מדיניות RLS ברמת שורה מוודאת שמשתמש רואה רק את הנתונים שלו
- **NEXT_PUBLIC vars:** רק `NEXT_PUBLIC_SUPABASE_URL` ו-`NEXT_PUBLIC_SUPABASE_ANON_KEY` חשופים לקליינט

### RTL

כל ה-UI בעברית, כיוון RTL. ב-Tailwind 4 — `dir="rtl"` על `<html>` בתוך `layout.tsx`. שימוש בקלאסות `start`/`end` (ולא `left`/`right`) לכל ה-layout.

### Server Actions

כל מוטציות נתונים (יצירה, עריכה, מחיקה, שינוי שלב) עוברות דרך Server Actions עם `'use server'`. כל Action מוודא auth פנימית לפני כל פעולה.

---

## 9. שאלות פתוחות ושלב הבא

### שאלות שנותרו לאימות

1. **אימות מחיר:** לפני בניית תשתית תשלום — לבדוק ₪39 מול קהל יעד (landing page + A/B, או Van Westendorp ל-20 פרילאנסרים).
2. **Mobile:** לא הוחלט — MVP מתחיל כ-responsive desktop-first. אחרי גיוס הפרילאנסרים הראשונים לבדוק על איזה מכשיר הם משתמשים בפועל.

### מה עדיין פתוח

1. **hero metric בבלבול רב-חודשי**: כרגע לא הוחלט לאיזה חודש ה-hero metric הראשי בקוקפיט מתייחס כשיש כמה חודשים עם ניצול שונה (הקרוב ביותר? הממוצע? זה עם הבעיה הכי חמורה?). צריך להחליט בשלב תכנון מסך ה-cockpit (שלב 4).

2. **המלצה בודדת מול כמה המלצות**: כרגע מנוע ההמלצות מחזיר הודעת-טקסט יחידה (RecommendationResult בודד), עם עדיפות ל-overload מעל כל שאר המצבים. הועלה רעיון עתידי אפשרי - להציג סטטוס לכמה חודשים קדימה בו-זמנית (במקום הודעה בודדת) - אבל זה שינוי אדריכלי גדול יותר (מבנה פלט שונה של המנוע + השפעה על ה-Free tier blur gate) שנדחה במפורש כרגע. הנתון הגולמי לכל החודשים כבר קיים וזמין דרך weeks[] מהמנוע, כך שהגרף החזותי (עמודות/Gantt) יכול להציג את כל החודשים גם היום - זו רק שאלה של האם גם שכבת הטקסט המילולי תהפוך לרב-חודשית בעתיד.

### צעדים ראשונים לפיתוח

1. Supabase project + schema migrations
2. Auth flow (Google OAuth + email)
3. Onboarding + הגדרת קיבולת
4. CRUD לקוחות + פרויקטים + רטיינרים
5. CRUD pipeline + Kanban + stage history
6. מנוע חישוב (`lib/calculations/`)
7. Cockpit UI (עמודות + Gantt + המלצה)
8. Upgrade gate + Supabase billing integration (Stripe)

### אימות MVP

גיוס 5–8 פרילאנסרים מגוונים, שימוש בכלי חודש-חודשיים עם נתונים אמיתיים.

**שאלת האימות המרכזית:** האם הם חוזרים לכלי מרצון, והאם הם פועלים בפועל לפי ההמלצות — לא רק "האם הנתונים מדויקים".
